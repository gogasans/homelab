# Python CLI tools managed via uv (https://docs.astral.sh/uv).
# Install with: make setup  (runs uv tool install for each)
# Or manually:  uv tool install ansible-lint==25.1.3 --with ansible==11.2.0 --python 3.12
ansible-lint==25.1.3
