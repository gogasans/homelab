apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - vault/cluster-secret-store.yaml
  # cert-manager ClusterIssuers and ExternalSecret (added in Phase 4 step 2)
  # traefik middlewares (added in Phase 4 step 4)
