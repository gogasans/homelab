---
# Full site playbook — orchestrates all phases in order.
# Safe to re-run: every role is idempotent.
#
# Usage:
#   ansible-playbook playbooks/site.yaml          # full run
#   ansible-playbook playbooks/site.yaml --check  # dry-run
#   ansible-playbook playbooks/site.yaml --tags prepare  # targeted run
- name: Prepare all nodes
  import_playbook: 01-prepare-nodes.yaml

- name: Install k3s
  import_playbook: 02-install-k3s.yaml
