---
galaxy_info:
  role_name: k3s-server
  description: >
    Installs and configures k3s as a control plane server. Writes config.yaml with
    Traefik and ServiceLB disabled. Waits for the API server and node to reach Ready.
  min_ansible_version: "2.15"

dependencies:
  - role: common
