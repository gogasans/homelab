---
galaxy_info:
  role_name: common
  author: homelab
  license: MIT
  description: >
    Prepares all k3s nodes: disables swap, loads kernel modules, applies sysctl
    parameters, installs required packages, and ensures qemu-guest-agent is running.
  min_ansible_version: "2.15"

dependencies: []
