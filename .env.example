# Copy this file to .env and fill in your values.
# .env is gitignored and is never committed.

# ── FluxCD bootstrap ───────────────────────────────────────────────────────────
# GitHub personal access token with repo (full) scope.
GITHUB_TOKEN=

# Your GitHub username.
GITHUB_OWNER=

# ── Kubernetes ─────────────────────────────────────────────────────────────────
# KUBECONFIG defaults to <repo-root>/.kube/config — no need to set this unless
# you want to override. Place your homelab kubeconfig at that path:
#KUBECONFIG=

# ── Cluster variables (substituted into manifests by Flux at reconcile time) ───
# These are NOT secrets — they are non-sensitive config kept out of git so the
# repo stays portable. Run `task configure-cluster-vars` to push them to the cluster.
ACME_EMAIL=
DOMAIN=

# ── Vault ──────────────────────────────────────────────────────────────────────
# Root token generated by vault-init.sh. Copy from 1Password after running vault-init.
# Used by `task vault-exec` and `task vault-seed-secrets`.
VAULT_ROOT_TOKEN=

# Secrets to be seeded into Vault by `task vault-seed-secrets`.
# These never leave your local machine — task reads them from here and writes them
# directly into Vault. Never put real values in .env.example.
CLOUDFLARE_API_TOKEN=

# ── OpenTofu ───────────────────────────────────────────────────────────────────
# Proxmox API token. Format: <user>@<realm>!<token-id>=<secret>
# Porxmox VE endpoint: Format: https://<ip>:<port>
PROXMOX_VE_API_TOKEN=
PROXMOX_VE_ENDPOINT=
