# Copy this file to .env and fill in your values.
# .env is gitignored and is never committed.

# ── FluxCD bootstrap ───────────────────────────────────────────────────────────
# GitHub personal access token with repo (full) scope.
GITHUB_TOKEN=

# Your GitHub username.
GITHUB_OWNER=

# ── Kubernetes ─────────────────────────────────────────────────────────────────
# KUBECONFIG defaults to <repo-root>/.kube/config — no need to set this unless
# you want to override. Place your homelab kubeconfig at that path:
#KUBECONFIG=

# ── OpenTofu ───────────────────────────────────────────────────────────────────
# Proxmox API token. Format: <user>@<realm>!<token-id>=<secret>
# Porxmox VE endpoint: Format: https://<ip>:<port>
PROXMOX_VE_API_TOKEN=
PROXMOX_VE_ENDPOINT=
